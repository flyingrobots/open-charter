.PHONY: all clean veryclean

# Convenience wrapper so editors configured to run `make` from the `paper/`
# directory still use the repo-root build (jobname/output dir/etc).
all clean veryclean:
	$(MAKE) -C .. $@
