% !TeX program = pdflatex
% --------------------------------------------------------------------
% The Open Charter — Commentary + Interludes
% --------------------------------------------------------------------

\documentclass[12pt]{article}

% 1) Packages first ---------------------------------------------------
% Some editors run `pdflatex paper/charter-commentary.tex` from the repo root,
% while the Makefile builds from within `paper/`. Use the supported `import`
% package plus a small base-dir detector so includes work from either working
% directory.
\usepackage{import}
\newcommand{\CharterBaseDir}{paper/}
\IfFileExists{./charter_preamble.tex}{\renewcommand{\CharterBaseDir}{./}}{}

\import{\CharterBaseDir}{charter_preamble.tex}

% 2) Global page/paragraph behavior next ------------------------------
% (Defined in the shared preamble.)

% 3) Custom macros/format tweaks after --------------------------------
% ---------- Part identification (for headers + title page) ----------
\def\SuiteDocName{Commentary \& Rationale}
\def\SuiteDocVersion{\CommentaryVersion}
\def\SuiteHeaderRight{\SuiteDocVersion\space—\space Commentary}
\def\SuitePdfTitle{\CharterTitle\space — Commentary \& Rationale\space (\SuiteDocVersion)}
\def\SuiteNormativeStatus{Non-normative (rationale and interpretive guidance).}
\def\SuitePrecedence{Core > Technical Standard > Commentary}
\def\SuiteNonDerogation{Commentary is non-binding and may not be used to reduce rights guaranteed by the Core.}
\def\SuiteVersionLock{Aligned with Core v\CoreVersion; citations are for context and interpretation.}

\begin{document}
\import{\CharterBaseDir}{charter_commentary_body.tex}
\end{document}
