% !TeX program = pdflatex
% --------------------------------------------------------------------
% The Open Charter — Consensus Edition
% Version metadata is defined in `charter_meta.tex` (see \CharterVersion).
% The Universal Edition includes Non-Human Biologics & Xenobiology.
% --------------------------------------------------------------------

\documentclass[12pt]{article}

% ---------- Page Geometry ----------
\usepackage[
  paper=a4paper,
  top=1.20in,
  bottom=1.20in,
  left=1.35in,
  right=1.35in,
  headheight=15pt
]{geometry}

\widowpenalty=10000
\clubpenalty=10000
\displaywidowpenalty=10000
\raggedbottom

% ---------- Typography ----------
\usepackage[T1]{fontenc}
\usepackage{libertinus}
\usepackage{microtype}

% ---------- PDF metadata + links ----------
\PassOptionsToPackage{hidelinks,hypertexnames=false}{hyperref}
\usepackage{hyperref}
\usepackage{bookmark}
\usepackage{orcidlink}

% ---------- Section Styling ----------
\usepackage{titlesec}

% TOC setup:
% - Keep section counters enabled so hyperref gets stable, unique anchors.
% - Hide section numbers in both headings and the TOC (article numbers are
%   part of the titles themselves via \Article{...}{...}).
\setcounter{secnumdepth}{2}
\setcounter{tocdepth}{2}
\makeatletter
\renewcommand*\numberline[1]{}
\makeatother

\titleformat{\section}
  {\normalfont\scshape\Large\centering}
  {}
  {0pt}
  {}
\titleformat{\subsection}
  {\normalfont\scshape\large}
  {}
  {0pt}
  {}

\titlespacing*{\section}{0pt}{2.8em}{1.2em}
\titlespacing*{\subsection}{0pt}{1.7em}{0.6em}

% ---------- Lists ----------
\usepackage{enumitem}
\setlist[itemize]{itemsep=0.4em, topsep=0.4em, leftmargin=2em}
\setlist[enumerate]{itemsep=0.4em, topsep=0.4em, leftmargin=2.2em}

% ---------- Header / Footer ----------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\thepage}

% ---------- Paragraph Feel ----------
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}

% ---------- Metadata ----------
% Keep content edits out of `main.tex`: update `charter_meta.tex` instead.
\makeatletter
% Some editors run `pdflatex paper/main.tex` from the repo root, in which case
% `\input{charter_meta}` and `\input{charter_body}` live under `paper/`.
% Add both locations to the input search path so either working directory works.
\providecommand\input@path{}
\g@addto@macro\input@path{{./}{paper/}}
\makeatother
\input{charter_meta}

% ---------- Macros ----------
\newcommand{\Article}[2]{%
  \subsection{Article #1 — #2}%
}
\newcommand{\FrontMatterSection}[1]{%
  \section{#1}%
}

% --- Canonical Identifiers ---
\newcommand{\AuthorORCID}{0009-0006-0025-7801}
\newcommand{\DocumentDOI}{10.5281/zenodo.18517806}
\newcommand{\ORCID}{%
  \orcidlink{\AuthorORCID}\,
  \href{https://orcid.org/\AuthorORCID}{\AuthorORCID}%
}

\newcommand{\DOI}{%
  DOI: \href{https://doi.org/\DocumentDOI}{\DocumentDOI}%
}

\begin{document}

% Keep content edits out of `main.tex`: update `charter_body.tex` instead.
\input{charter_body}

\end{document}
