% !TeX program = pdflatex
% --------------------------------------------------------------------
% The Open Charter — Consensus Edition
% Version metadata is defined in `charter_meta.tex` (see \CharterVersion).
% The Universal Edition includes Non-Human Biologics & Xenobiology.
% --------------------------------------------------------------------

\documentclass[12pt]{article}

% 1) Packages first ---------------------------------------------------
% Some editors run `pdflatex paper/main.tex` from the repo root, while the
% Makefile builds from within `paper/`. Use the supported `import` package plus
% a small base-dir detector so includes work from either working directory.
\usepackage{import}
\newcommand{\CharterBaseDir}{paper/}
\IfFileExists{./charter_preamble.tex}{\renewcommand{\CharterBaseDir}{./}}{}

\import{\CharterBaseDir}{charter_preamble.tex}

% 2) Global page/paragraph behavior next ------------------------------
% (Defined in the shared preamble.)

% 3) Custom macros/format tweaks after --------------------------------
% ---------- Suite identification (for headers + title page) ----------
\def\SuiteDocName{Suite (Core + Technical Standard + Commentary)}
\def\SuiteDocVersion{\SuiteReleaseVersion}
\def\SuiteHeaderRight{\SuiteDocVersion\space—\space Suite}
\def\SuitePdfTitle{\CharterTitle\space Suite\space (\SuiteDocVersion)}
\def\SuiteNormativeStatus{Mixed: Core and Technical Standard are normative; Commentary is non-normative.}
\def\SuitePrecedence{Core > Technical Standard > Commentary}
\def\SuiteNonDerogation{Technical Standard may not reduce protections guaranteed by Core.}
\def\SuiteVersionLock{Parts are released as a coordinated suite; cite each part by name and version.}

\begin{document}

% Keep content edits out of `main.tex`: update `charter_body.tex` instead.
\import{\CharterBaseDir}{charter_body.tex}

\end{document}
